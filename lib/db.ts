// Database client — add Prisma or Drizzle here when a DB is connected.
// All queries MUST use parameterized statements (ORM handles this automatically).
//
// Example with Prisma (SQL injection safe — never use string concatenation):
//   const user = await prisma.user.findUnique({ where: { email } });   // ✅ safe
//   db.query(`SELECT * FROM users WHERE email = '${email}'`);           // ❌ never do this

export {};
